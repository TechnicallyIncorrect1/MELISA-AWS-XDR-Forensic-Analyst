name: CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: MELISA Status
      run: echo "✅ MELISA-AWS-XDR-Forensic-Analyst listo para ejecución"

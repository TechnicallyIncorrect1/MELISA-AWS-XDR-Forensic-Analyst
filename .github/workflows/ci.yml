name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: MELISA Repository Status
        run: echo "✅ MELISA-AWS-XDR-Forensic-Analyst is now clean and ready"
